# Use Node.js provider with pnpm
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["corepack enable", "corepack prepare pnpm@latest --activate"]

[phases.build]
cmds = ["pnpm install", "pnpm run build"]

[start]
cmd = "npx serve dist -s -l $PORT"
